[pytest]
testpaths = .
norecursedirs = venv .git __pycache__
# The async agent/orchestrator tests are plain `async def test_*` functions.
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestCollectionWarning
